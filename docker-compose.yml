version: "3.9"
services:
  nostrchan:
    container_name: nostrchan
    build:
      context: "./"
      dockerfile: "./Dockerfile"
    volumes:
      - "./:/var/bot/"
    tty: true
    command: python bot.py
    restart: always
