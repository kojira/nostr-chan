version: "3.9"
services:
  subscriber:
    container_name: bot
    build:
      context: "./"
      dockerfile: "./Dockerfile"
    volumes:
      - "./:/var/bot/"
    tty: true
    command: python bot.py
    restart: always
  # tester:
  #   container_name: tester
  #   build:
  #     context: "./"
  #     dockerfile: "./Dockerfile"
  #   volumes:
  #     - "./:/var/bot/"
  #   tty: true
  #   command: python gpt_test.py
